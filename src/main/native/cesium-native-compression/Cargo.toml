[package]
name = "cesium-native-compression"
version = "1.0.0"
edition = "2021"

[lib]
name = "cesium_compression"
crate-type = ["cdylib"]

[dependencies]
zstd = "0.13"
lz4_flex = "0.11"
jni = "0.21"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
